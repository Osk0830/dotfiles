# VS Code Profiles

Current recommendation:

- Keep **settings.json** under version control.
- Keep **extensions.txt** under version control.
- Keep **keybindings.json** only if you customize shortcuts.
- Exporting/importing a VS Code Profile is optional.

If you later create separate profiles (PHP, React, Python, etc.),
you can export them from:

Preferences → Profiles → Export Profile
